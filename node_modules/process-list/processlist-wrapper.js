for (let file of [
    `${__dirname}/build/Release/processlist.node`,
    `${__dirname}/processlist_${process.versions.electron ? 'electron' : 'node'}_${process.arch}_${process.versions.modules}.node`
]) {
    try {
        module.exports = require(file);
        return;
    } catch (_) {
        // Ignore
    }
}

throw Error(`[toolbox] process-list: No build found (platform=${process.versions.electron ? 'electron' : 'node'}, arch=${process.arch}, modulesVer=${process.versions.modules})`);
