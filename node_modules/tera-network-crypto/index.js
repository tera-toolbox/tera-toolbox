for (let file of [
    `${__dirname}/bin/tera_network_crypto_${process.versions.modules}.node`
]) {
    try {
        module.exports = require(file);
        return;
    } catch (_) {
        // Ignore
    }
}

throw Error(`[toolbox] tera-network-crypto: No build found (platform=${process.versions.electron ? 'electron' : 'node'}, arch=${process.arch}, modulesVer=${process.versions.modules})`);
